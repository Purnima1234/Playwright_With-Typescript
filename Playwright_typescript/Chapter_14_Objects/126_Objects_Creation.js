let c={name: "Purnima"};

let d={name: "Purnima"};

console.log(c==d); //output: false. This is because c and d are two different objects in memory. 
// They have the same properties and values but they are not the same object in memory. 
// So, when we compare c and d using the == operator, it returns false because they are not the same object in memory.

