console.log(typeof("Hello"));
 let str= "Hello world";
 //console.log(str.length); //length always start from 1
//console .log (str[0]);
//console.log(str.at(-1)); //at method is used to access the last character of the string by using negative index. -1 means the last character of the string.

//index starts from 0 length starts form 1

//console.log(str.charAt(0)); //charAt method is used to access the character at a specific index. It takes the index as an argument and returns the character at that index. If the index is out of range, it returns an empty string.

console.log(str.charCodeAt(0));
